public class Player {

    //For buying property: Check if the class is Rail by: .instanceof(Rail)
    // If yes, we do not need to check the full set of color to buy rail
    // Yep, that is a good idea, we no longer need to assign the color attribute to the rail class if .instanceof(Rail) works as intended
}
